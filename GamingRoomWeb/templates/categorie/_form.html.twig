{{ form_start(form,{'attr':{'novalidate':'novalidate'}}) }}
<div class="form-group">
    {{ form_widget(form.nomcategorie,{'attr':{'placeholder':'Nom catégorie'}}) }}
    {% if form_errors(form.nomcategorie) %}
        <div class="alert alert-danger" role="alert">
            {{ form_errors(form.nomcategorie) }}
        </div>
    {% endif %}
</br>
</br>
    <button class="btn btn-primary" style="width: 100px">{{ button_label|default('Save') }}</button>
</div>
{{ form_end(form) }}

